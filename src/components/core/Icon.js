import {createIconSet} from 'react-native-vector-icons';

const glyphMap = require('../../assets/icons/remixicon.glyphmap.json');
const Icon = createIconSet(glyphMap, 'RemixIcon', 'remixicon.ttf');

export default Icon;
